<script setup lang="ts">
// Full-bleed shell for the /auth login page (no sidebar). The page owns the
// two-column split so the hero can run edge-to-edge.
</script>

<template>
  <div class="min-h-screen bg-app">
    <slot />
  </div>
</template>
